import React from 'react';
import { EditorComparison } from '../components/EditorComparison';

const EditorComparisonPage: React.FC = () => {
  return <EditorComparison />;
};

export default EditorComparisonPage;
